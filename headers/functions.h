#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <...>
#include <...>
#include <...>

void draw( table* Table );

void exit();

void new_table( char** arguments );
void assign( char** arguments );
void del( char** arguments);

void done( char** arguments );
void undone( char** arguments );

void rename( char** arguments );

#endif
